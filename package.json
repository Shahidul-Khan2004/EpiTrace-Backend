{
  "name": "hackathon-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "cli"
  ],
  "scripts": {
    "server": "npm --workspace server run dev",
    "server:start": "npm --workspace server run start",
    "cli": "npm --workspace cli run dev",
    "cli:start": "npm --workspace cli run start"
  }
}
